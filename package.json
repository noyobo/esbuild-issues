{
  "name": "chunk-bug",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && node ./scripts/build.js"
  },
  "dependencies": {
    "esbuild": "^0.18.2"
  },
  "devDependencies": {}
}
